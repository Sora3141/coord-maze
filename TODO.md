# これからやりたいこと

思いついた順ではなく、手を付けやすい順に並べてあります。
数字は 2026-09-24 時点の実測値です。

## 1. ~~押せないサイズを無くす~~ → 10 次元 10 マスまで選べるようにした (2026-09-24)

120 万状態を超える盤面は、階層的な暗黙の迷路 ([src/hmaze.js](src/hmaze.js)) で作るようにした。
壁を持たずに聞かれたところだけ作るので、10 次元 10 マス (100 億状態) でも生成 30ms・メモリ数 MB。
すべての状態に行ける輪のない迷路のまま。120 万状態以下は前と同じ迷路 (MazeND) を出す
(README の「大きい盤面: 階層的な暗黙の迷路」)。

難しさは今までの迷路の延長にしてある (道筋を先に引くだけの素直な作り方。盤面が大きいほど難しい)。
素朴な遊び方では、7 次元 8 マス以上はどれも 30 万手以内には解けない (今までの 7 次元 7 マスも同じ)。

残っている課題:

- **大きい盤面を遊びやすくするか。** 扉をそろえる・本道・葉を細かくしすぎない、の 3 つを入れると、
  10 次元 10 マスでも 20 シード中 20 が 30 万手以内に解ける (中央値 9,642 手)。ただし今までの 7 次元 7 マスより
  易しくなり、大きさと難しさが逆転するので使っていない (README の「使っていない、遊びやすくする設定」)。
  「難しさを選べる」ようにして、ここを切り替えるのも手
- **人の遊び方で確かめる。** 数字は「右を先に試す素朴な遊び方」で測ったもの。人が遊んで、
  大きい盤面の手応えを見たい

## 2. アカウントとグローバルランキング（構想）

自分のアカウント（Google アカウントなど）を持って、**グローバルランキング**を出したい。
同じサイズ・同じシードでの手数を競えるように。

いまは GitHub Pages で配信している静的サイト（サーバーなし）なので、着手するときは
「静的サイトのままで済む構成（Firebase / Supabase など）か、サーバーを持つか」を最初に選ぶ。

クリアの記録そのものは手元にある（[src/records.js](src/records.js) が localStorage に
1 件ずつ残している）。1 件を「どこにでも送れる平らな形」にしてあるので、
そのまま送り先を足す形で載せ替えられる。
