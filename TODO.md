# これからやりたいこと

思いついた順ではなく、手を付けやすい順に並べてあります。
数字は 2026-09-18 時点の実測値です。

## 1. UI の整理（すぐできる）

- **チュートリアルの効果音 ON/OFF ボタンを消す**
  本編では設定（ハンバーガー）の中へ移したが、チュートリアルにはハンバーガーがないので
  操作バーに残っている。そもそもチュートリアルには要らない。
  → [tutorial.html](tutorial.html) の `#btn-sound` と、[src/tutorial.js](src/tutorial.js) 側の
  参照（初期化とクリック）を一緒に外す
- **引き出しの中の「はじめての方へ」の節を消す**
  引き出しのいちばん上にチュートリアルへのボタンがあるので二重になっている。
  → [index.html](index.html) の該当する `<details class="sec">` を削除

## 2. 大きい盤面の見た目

次元数・マス数が多いと、盤面が**画面からはみ出す**／**中央からずれる**。

いまの作り:

- `CoordBoard.fit()`（[src/coordboard.js](src/coordboard.js)）が置き場所の幅からマスの大きさを
  決める（下限 30px）。入り切らないときだけ `#board` に `.scrolls` を付けて横スクロールにする
- 中央そろえは「行頭の軸名と同じ幅の余白を右にも取る」ことで実現している。
  ただし `.scrolls` のときは中央そろえをやめているので、**大きい盤面では中央に来ない**
- 縦方向は未対応。10 次元だと行が 10 本あり、縦にはみ出す

候補: マスの下限を割ってでも収める／横スクロール中も中央寄せにする／行の高さを詰める。
直したら 390px と 320px の幅で、7 次元 7 マスなど大きい盤面を実際に触って確かめる。

## 3. 押せないサイズを無くす

状態数の上限 `MAX_STATES = 1_200_000`（[src/puzzle.js](src/puzzle.js)）を超える組み合わせは、
設定パネルで押せなくしてある。

| 次元数 | 押せるマス数 | 押せない |
|---|---|---|
| 2〜6 | 2〜10 | なし |
| 7 | 2〜7 | 8, 9, 10 |
| 8 | 2〜5 | 6〜10 |
| 9・10 | 2〜4 | 5〜10 |

**方針: 選択肢を減らす（両方 7 までにする）のではなく、アルゴリズムや実装を良くして
遊べるサイズを増やす。**

定数を上げるだけでは駄目で、上限は 2 つの理由で決まっている。

1. **持てるか** — 壁を状態数ぶんの配列で持っている。7 次元 7 マス = 82 万状態で生成 130ms
2. **遊び切れるか** — すべての状態に行ける迷路である以上、探索量が状態数に比例する。
   素朴に遊ぶと 1 万状態で 2,000〜4,000 手、100 億状態では 30 万手打っても着かない
   （README の「上限を 120 万状態にしている理由」参照）

過去に「道と枝で作る疎な迷路」と「親を決める関数だけを置く暗黙の全域木」を実装して
測ったが、前者は行けない状態だらけ、後者は遊び切れないため不採用にした。
次に挑むなら **2 の「遊び切れるか」をどう設計するか**が本丸。

## 4. クリアの記録

- クリアした**盤面サイズとシード値**を残す（あとで同じ問題をもう一度遊べるように）
- **サイズごとに何回クリアしたか**を数える。メモも残せるとよい

土台: [src/save.js](src/save.js) が localStorage に「進みぐあい」を保存している
（キー `coordmaze.game`）。記録は別キー（例 `coordmaze.records`）に分けるのが素直。
クリアの瞬間は `CoordMaze.#win()` で、手数・最短手数・タイム・シードが揃っている。

あとからアカウントに載せ替えられるよう、データの形を先に決めておく。

## 5. アカウントとグローバルランキング（構想）

自分のアカウント（Google アカウントなど）を持って、**グローバルランキング**を出したい。
同じサイズ・同じシードでの手数を競えるように。

いまは GitHub Pages で配信している静的サイト（サーバーなし）なので、着手するときは
「静的サイトのままで済む構成（Firebase / Supabase など）か、サーバーを持つか」を最初に選ぶ。
